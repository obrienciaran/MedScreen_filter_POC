# Dependencies for medscreen-poc. Mirrors pyproject.toml, which remains the source of truth.
# Install the package itself with: pip install -e .
# (or the full real-backend setup: pip install -e ".[dev,embed,gemini]")

# Core: required for the filter and validation tool to run on offline stubs.
httpx>=0.27
duckdb>=1.1
pydantic>=2.7
pyyaml>=6.0

# Optional: semantic re-ranking (MEDSCREEN_EMBED_BACKEND=sbert), the `embed` extra.
sentence-transformers>=3.0

# Optional: LLM providers for claim extraction and stance judgement. Install the one you use.
google-genai>=0.3
# anthropic>=0.40
# openai>=1.40

# Dev: tests.
pytest>=8.0
